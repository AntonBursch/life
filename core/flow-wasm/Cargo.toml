[package]
name = "flow-wasm"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "wasm bindings for the flow crate."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
flow = { path = "../flow" }
wasm-bindgen = "=0.2.114"
